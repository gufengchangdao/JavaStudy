类的五大成员：
    属性、方法、代码块、构造器、内部类
内部类：
    一个类的内部又完整嵌套了另一个类结构。被嵌套的类又称为内部类，嵌套其他类的类称为外部类
    内部类的作用范围，可调用的属性、方法，可添加的修饰符都与它定义的位置有关，可以把它就看成一个变量，变量可以有的它也可以有，不可以有的就没有

分类：
    定义在外部类局部位置上(比如方法内):
        局部内部类(有类名)
        匿名内部类(没有类名)
    定义在外部类的成员位置:
        成员内部类(没用static修饰)
        静态内部类(使用static修饰)


